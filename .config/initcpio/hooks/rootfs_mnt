# vim: set ft=sh:

run_hook () {
    mount_handler="rootfs_mount_handler"
}

rootfs_mount_handler () {
    new_root_mp="${1}"

    mkdir -p /run/disk0
    mount /dev/sda1 /run/disk0
    mount -t squashfs /run/disk0/test.sfs ${new_root_mp}
}

